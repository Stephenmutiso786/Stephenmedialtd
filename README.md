# Setup and Publish Instructions

## Setup
1. Clone the repository: `git clone https://github.com/Stephenmutiso786/Stephenmedialtd.git`
2. Navigate to the project directory: `cd Stephenmedialtd`
3. Install dependencies: `npm install`

## Publish
1. Build the application: `npm run build`
2. Start the application: `npm start`